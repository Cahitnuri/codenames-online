[build]
builder = "nixpacks"
buildCommand = "bash -c 'cd $(git rev-parse --show-toplevel) && npm install && npm run build:client'"

[deploy]
startCommand = "bash -c 'cd $(git rev-parse --show-toplevel) && npm run start:client'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
